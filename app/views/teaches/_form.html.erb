<%= form_with(model: teach, local: true) do |form| %>
  <% if teach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(teach.errors.count, "error") %> prohibited this teach from being saved:</h2>

      <ul>
      <% teach.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :dept_id %>
    <%= form.number_field :dept_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
